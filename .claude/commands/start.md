Move the Trello card named "$ARGUMENTS" from Backlog to In Progress on the Holoverse board (ID: 69ed18499eb64f3f34e38375).

Steps:
1. Search the Backlog list for a card matching the given name (fuzzy match is fine — pick the closest one and confirm with the user if ambiguous).
2. Move it to the In Progress list.
3. Set today as the start date.
4. Confirm the move with the card name and a link.
