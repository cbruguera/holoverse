Move the Trello card named "$ARGUMENTS" to Done on the Holoverse board (ID: 69ed18499eb64f3f34e38375).

Steps:
1. Search all active lists for a card matching the given name (fuzzy match is fine — confirm with the user if ambiguous).
2. Move it to the Done list and mark due date complete if one is set.
3. If the current git branch or a recent commit message is clearly related to this card, add a short comment to the card with the commit hash or branch name.
4. Confirm the move with the card name and a link.
