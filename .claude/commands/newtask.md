Create a new card in the Backlog list on the Holoverse board (ID: 69ed18499eb64f3f34e38375) based on "$ARGUMENTS".

Steps:
1. Use the description to infer the best label(s) from the available labels:
   - Smart Contracts — Solidity, contracts, ERC, deploy, on-chain
   - Indexer — Ponder, indexer, events, subgraph
   - Frontend — UI, web, app, client, React
   - Infrastructure / CI — CI, pipeline, config, monorepo, tooling, scripts, env
   - Documentation — docs, spec, design, architecture
   - Bug — bug, fix, broken, error, regression
2. Create the card with a clear name and a brief description.
3. Confirm with the card name, assigned labels, and a link.
