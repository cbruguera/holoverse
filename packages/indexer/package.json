{
  "name": "@holoverse/indexer",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "echo \"Indexer stub — Ponder not yet configured\" && exit 0",
    "build": "echo \"Indexer stub — Ponder not yet configured\" && exit 0",
    "test": "echo \"No tests yet\" && exit 0"
  }
}
